import pandas as pd
import numpy as np


chat_id = 730773348 t ID, не меняйте название переменной

def solution(x: np.ndarray) -> float:
    # Измените код этой функции
    # Это будет вашим решением
    # Не меняйте название функции и её аргументы
    t = 34
    return np.mean(x)/(n*t)
